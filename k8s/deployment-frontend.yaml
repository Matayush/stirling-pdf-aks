apiVersion: apps/v1
kind: Deployment
metadata:
  name: stirling-pdf-frontend
  namespace: stirling-pdf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stirling-pdf-frontend
  template:
    metadata:
      labels:
        app: stirling-pdf-frontend
    spec:
      containers:
      - name: stirling-pdf-frontend
        image: stirlingtools/stirling-pdf:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: stirling-pdf-frontend-config
        env:
        - name: MODE
          value: "FRONTEND"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"